{
  "name": "worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker mínimo — sitemap, headers e lugar reservado para APIs futuras.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "typescript": "~5.6.0",
    "wrangler": "^3.80.0"
  }
}
